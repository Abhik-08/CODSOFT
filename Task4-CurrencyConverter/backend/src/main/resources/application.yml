spring:
  application:
    name: currency-converter-backend

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html

exchange-rate-api:
  base-url: "https://open.er-api.com/v6/latest"
